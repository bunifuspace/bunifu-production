# Node & Composer dependencies
/node_modules
/vendor

# Environment variables & secrets
.env
.env.backup
.env.production
*.key

# Application cache & logs
/bootstrap/cache/
/storage/logs/
/storage/framework/

# OS generated files
.DS_Store
Thumbs.db

# The build directory (we will push the contents, not the directory itself)
# Adjust this name if your build process outputs to a different folder (e.g., 'dist', 'build')
/public/build
